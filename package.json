{
  "name": "facebook-emoji-suggest",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run build.ts --watch",
    "build": "bun run build.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "bun-types": "^1.1.0",
    "typescript": "^5.3.0"
  }
}
